import React from 'react';
import './Loading.css'; // Import the CSS file

const Loading = () => {
  return (
    <div className="loading-container">
      <div className="loading"></div>
    </div>
  ); // Render the loading animation centered on the page
};

export default Loading;
